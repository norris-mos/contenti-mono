"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var h5p_server_1 = require("@lumieducation/h5p-server");
var debug_1 = __importDefault(require("debug"));
var log = (0, debug_1.default)('h5p:SharedStateServer:checkPermissionsAndInjectContentContext');
/**
 * Checks the permissions and injects information about the content object into
 * the context.
 */
exports.default = (function (getPermissionForUser, getLibraryMetadata, getContentMetadata, getContentParameters) {
    return function (context, next) { return __awaiter(void 0, void 0, void 0, function () {
        var contentId, agent, user, permission, contentMetadata_1, libraryMetadata, params;
        var _a, _b, _c;
        return __generator(this, function (_d) {
            switch (_d.label) {
                case 0:
                    contentId = context.id;
                    agent = context.agent.custom;
                    user = agent.user;
                    // Allow all operations by the server
                    if (agent.fromServer) {
                        return [2 /*return*/, next()];
                    }
                    if (!user && !agent.fromServer) {
                        return [2 /*return*/, next(new Error('No user data in submit request'))];
                    }
                    return [4 /*yield*/, getPermissionForUser(user, contentId)];
                case 1:
                    permission = _d.sent();
                    if (!permission) {
                        log('User %s tried to access content without proper permission.', user.id);
                        return [2 /*return*/, next('You do not have permission to access this content.')];
                    }
                    agent.permission = permission;
                    log('User %s (%s) is accessing %s with access level %s', user.id, user.name, contentId, permission);
                    if (!contentId) return [3 /*break*/, 5];
                    return [4 /*yield*/, getContentMetadata(contentId, user)];
                case 2:
                    contentMetadata_1 = _d.sent();
                    return [4 /*yield*/, getLibraryMetadata(contentMetadata_1.preloadedDependencies.find(function (d) { return d.machineName === contentMetadata_1.mainLibrary; }))];
                case 3:
                    libraryMetadata = _d.sent();
                    if (((_a = libraryMetadata.requiredExtensions) === null || _a === void 0 ? void 0 : _a.sharedState) !== 1) {
                        log('Library %s uses unsupported shared state extension: The library requires v%s but this application only supports v1.', h5p_server_1.LibraryName.toUberName(libraryMetadata), (_b = libraryMetadata.requiredExtensions) === null || _b === void 0 ? void 0 : _b.sharedState);
                        // Unknown extension version ... Aborting.
                        return [2 /*return*/, next(new Error("Library ".concat(h5p_server_1.LibraryName.toUberName(libraryMetadata), " uses unsupported shared state extension: The library requires v").concat((_c = libraryMetadata.requiredExtensions) === null || _c === void 0 ? void 0 : _c.sharedState, " but this application only supports v1.")))];
                    }
                    return [4 /*yield*/, getContentParameters(contentId, user)];
                case 4:
                    params = _d.sent();
                    agent.params = params;
                    agent.ubername = h5p_server_1.LibraryName.toUberName(libraryMetadata);
                    agent.libraryMetadata = libraryMetadata;
                    _d.label = 5;
                case 5:
                    next();
                    return [2 /*return*/];
            }
        });
    }); };
});
//# sourceMappingURL=checkPermissionsAndInjectContentContext.js.map